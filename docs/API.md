# HTTP API (v1)

Base path `/api/v1`. All responses are JSON except the competition file route,
which returns `text/csv`. Requests carry `Authorization: Bearer <token>` unless
the service was started with `--auth none`.

Identifiers generated by the service match `[a-z0-9]{1,40}`. Anything else is
rejected before it reaches the filesystem.

---

### `GET /health`
No authentication. Returns queue depth and whether auth is required.

```json
{"status":"ok","queued":0,"running":1,"auth_required":true,"time":"2026-09-18T13:32:53Z"}
```

### `POST /instances`
`multipart/form-data`. Exactly eight parts, named for the eight canonical
filenames. Total payload capped at 32 MiB; each file capped at 500 000 rows.

**202-style success (200):**
```json
{"instance_id":"r5tec4obg48c404p","accepted":true,
 "summary":{"activities":54,"contracts":14,"locations":76,"horizon_weeks":30,
            "horizon_start":"2027-01-04","input_hash":"52af39d3…",
            "exclusive_pairs":58,"total_accesses":192}}
```

**Rejected (422)** — every problem is reported, not just the first:
```json
{"instance_id":null,"accepted":false,"error_count":2,
 "errors":[{"file":"06_PARAMETERS.csv","row":2,"field":"horizon_start",
            "message":"expected a date as YYYY-MM-DD, found \"2027-13-45\""}]}
```

**Wrong number of files (400)** — lists the expected names.

### `POST /instances/demo`
Loads the bundled public instance, when the service was started with
`--public-instance`. Responds as above with `"bundled": true`. Provided so a
reviewer can exercise the workflow; it is never presented as an upload.

### `GET /instances/{id}/detail`
Full model for the interface: every location with its supply and chain position,
every contract, and every activity with its expanded `occupied` set and its
`closure` set. Canonical identifiers throughout.

### `POST /jobs?instance_id=&scenario=&seconds=`
`scenario` is `A`, `B`, `C` or `all` (default `all`). `seconds` is clamped to
`[1, --max-seconds]`. Returns the job record. `429` when the queue is full.

```json
{"job_id":"llywdd69a90ss3jm","instance_id":"r5tec4obg48c404p","scenario":"all",
 "state":"queued","created_at":"…","started_at":"","finished_at":"",
 "error":"","progress":[]}
```

`state` ∈ `queued` · `running` · `done` · `failed` · `cancelled`.

### `GET /jobs/{id}` · `GET /jobs/{id}/log`
Job record, and the worker's captured output.

### `POST /jobs/{id}/cancel`
Signals the worker. A queued job becomes `cancelled` immediately; a running one
stops and keeps the best complete plan already written.

### `GET /jobs/{id}/validation/{A|B|C}`
The conformance report for one scenario, as written by the worker:

```json
{"scenario":"A","feasible":true,"checker":"trackaccess-check/0.1",
 "checker_is_official_validator":false,
 "input_hash":"52af39d3…","hard_violations":[],
 "soft_scores":{"overrun_days_total":28,"contracts_overrunning":3,
   "earliness_days_total":21,"excess_access_nights_total":0,"eclo_nights_total":0,
   "priority_overrun":{"1":0,"2":0,"3":28},"priority_weighted_score":32.2,
   "objective_score":32.2,"formula_version":"PS1-README@966c976"},
 "detail":{"nights_scheduled":192,"eclo_nights":0,"capacity_hotspots":191}}
```

`objective_score` and `formula_version` appear only when `feasible` is true.
`checker_is_official_validator` is always `false` and is present so no consumer
can mistake this report for the organisers' validator output.

### `GET /jobs/{id}/files/{A|B|C}/{FILE}`
`FILE` must be one of `SCHEDULE_ACCESS.csv`, `SCHEDULE_OCCUPANCY.csv`,
`RESULTS.csv`. Any other name is `404`, including names that exist on disk.

---

## Errors

| Status | Meaning |
| --- | --- |
| 400 | malformed request (unknown scenario, wrong file count) |
| 401 | missing or wrong bearer token |
| 404 | unknown id, or a path that does not name a permitted resource |
| 422 | the instance parsed but breaks the schema — body carries every problem |
| 429 | solve queue full |

## Versioning

The path carries the version. A breaking change to a response shape takes a new
path prefix; fields may be added to existing responses without one.
